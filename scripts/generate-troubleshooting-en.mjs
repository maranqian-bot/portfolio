import { readFile, writeFile } from 'node:fs/promises';
import { join } from 'node:path';

const root = join(new URL('..', import.meta.url).pathname);
let html = await readFile(join(root, 'zh/troubleshooting.html'), 'utf8');
const copy = {
  '问题排查 | MA RanQian 作品集':'Troubleshooting | MA RanQian Portfolio','关于我':'About Me','项目':'Projects','最终项目':'Final Project','迷你项目':'Mini Project','问题排查':'Troubleshooting','协作方式':'Collaboration','中文':'中文','问题排查与解决':'Problem Solving and Diagnosis','复现问题并逐步缩小原因的记录':'A record of reproducing problems and narrowing down their causes','我在负责 Mapingo AI 会话辅导与 CareerWave 认证、支付领域时遇到的问题，按复现条件、原因分析、解决方案和重新验证进行记录。':'I documented the problems I encountered while working on Mapingo AI conversation coaching and CareerWave authentication and payment domains, organized by reproduction conditions, cause analysis, solutions, and revalidation.','全部':'All','性能':'Performance','认证':'Auth','支付':'Billing','语音':'Speech',
  '最终项目 · CareerWave':'Final Project · CareerWave','迷你项目 · Mapingo':'Mini Project · Mapingo','JWT 认证过滤器异常被返回为 500':'JWT authentication-filter exception returned as 500','问题':'Problem','原因':'Cause','解决方案':'Solution','结果':'Result','调用受过期或篡改令牌保护的 API 时，返回的不是客户端预期的 401，而是 500。前端无法区分登录过期和服务器错误，重新登录提示流程因此失效。':'When an API protected by an expired or tampered token was called, it returned 500 instead of the 401 the client expected. The frontend could not distinguish an expired login from a server error, so the re-login guidance flow broke.','起初我以为是全局异常处理配置的问题并检查了 Controller Advice，但 JWT 验证实际上在到达 DispatcherServlet 前的 Security Filter Chain 中执行。因此过滤器内部产生的令牌过期、签名不匹配异常无法被 Controller Advice 捕获，在没有单独处理的情况下像 RuntimeException 一样继续传播，最终转换成 500。':'I initially suspected the global exception-handling configuration and checked Controller Advice, but JWT validation actually ran in the Security Filter Chain before reaching DispatcherServlet. Token-expiration and signature-mismatch exceptions raised inside the filter could not be caught by Controller Advice; without dedicated handling, they propagated like RuntimeException and became 500.','在 JWT 过滤器中，我将令牌验证失败原因整理为 AuthenticationException 类型，并在 AuthenticationEntryPoint 中明确返回 401 状态码和错误消息。为避免暴露过多细节，我分离了面向客户端的提示消息与服务器日志。':'In the JWT filter, I normalized token-validation failures as AuthenticationException types and explicitly returned the 401 status code and error message from AuthenticationEntryPoint. I separated client-facing guidance from server logs to avoid exposing excessive detail.','我分别复现令牌缺失、过期和篡改请求，确认它们全部返回 401。正常令牌请求保持原有 API 流程，前端也可以依据 401 处理重新登录提示。':'I reproduced missing-token, expired-token, and tampered-token requests separately and confirmed that all returned 401. Valid-token requests kept the existing API flow, and the frontend could use 401 to show the re-login guidance.','刷新时因 refresh 竞争而退出登录':'Logout caused by a refresh race on page reload','在页面刷新或 OAuth 回调后、access token 尚未恢复到内存的瞬间，多个组件同时尝试恢复会话，导致用户立即退出登录。即使存在有效的 refresh Cookie，会话也会因进入页面的时机而中断，造成不稳定的体验。':'At the moment after a page reload or OAuth callback when the access token had not yet been restored to memory, multiple components tried to restore the session at once and logged the user out. Even with a valid refresh Cookie, the session could be interrupted by page-entry timing, creating an unstable UX.','在收到 401 响应或进入初始受保护路由时，每个请求都会独立地':'When a 401 response was received or an initial protected route was entered, each request independently','调用，服务器同时启用了 refresh token rotation 和重复使用检测。如果使用同一个 Cookie 几乎同时发起两次以上重新签发，系统可能判断已轮换的令牌被再次使用，从而废弃整个会话。':'called the endpoint, while the server applied refresh-token rotation and reuse detection. If the same Cookie triggered two or more nearly simultaneous reissues, the system could treat the rotated token as reused and revoke the entire session.','我将前端会话恢复逻辑合并为 single-flight，让后续请求加入正在进行的 refresh Promise。同时在 OAuth 回调页面通过':'I grouped the frontend session-restoration logic into a single-flight so later requests join the refresh Promise already in progress. On the OAuth callback page, I also used','固定顺序，确保 refresh Cookie 轮换完成后再跳转，关闭新页面使用旧 Cookie 再次发起 refresh 的竞争窗口。':'to enforce the order and wait for refresh Cookie rotation before navigating, closing the race window in which a new page could refresh again with the old Cookie.','即使调用':'Even if','同时多次调用，也通过测试固定为只发出一次 refresh 请求。OAuth 回调后同样验证轮换完成前不跳转，刷新后立即退出登录的现象消失。':'is called multiple times concurrently, tests guarantee that only one refresh request is sent. We also verify that navigation waits for rotation after OAuth callbacks, eliminating the immediate logout after reload.','社交登录用户支付时返回 404':'404 returned when a social-login user pays','QA 发现，Kakao 社交登录用户尝试支付时，前端没有进入支付页面而是收到 404。用户无法知道支付准备失败的原因，后端也走上了与实际原因不符的异常路径。':'QA found that when a Kakao social-login user attempted payment, the frontend received 404 instead of entering the payment page. The user could not see why payment preparation failed, and the backend followed an exception path unrelated to the actual cause.','部分社交账号的邮箱为空，该值违反了':'Some social accounts had an empty email, which violated the','约束，并在 DB 保存阶段产生了':'constraint and produced the following during DB persistence:','但该异常流入原本用于恢复并发请求冲突的 catch 代码块，实际的约束违反被当成订单不存在处理，最终变成了 404。':'The exception then flowed into a catch block originally intended to recover from concurrent-request conflicts. The actual constraint violation was treated as a missing order and became 404.','我在支付 INSERT 前明确验证邮箱是否存在，在前端阻止约束违反并转换为业务错误。同时修改前端错误分支和消息，将支付进入失败处理为需要补充用户信息，而不是 404。':'I explicitly validated email presence before the payment INSERT, blocking the constraint violation at the frontend and converting it into a business error. I also updated the frontend error branch and message so payment-entry failure is handled as a need for additional user information, not 404.','使用没有邮箱的社交账号重新复现支付路径时，不再被误判为 404。QA 阶段用于确认原因的 DB 直接查询结果与响应流程保持一致，用户也能收到正确的支付失败原因。':'When the payment path was reproduced with a social account missing an email, it was no longer misidentified as 404. The DB lookup used during QA and the response flow now agree, and users receive the correct reason for payment failure.',
  'LLM 响应格式变化导致解析失败':'Parsing failure caused by an LLM response-format change','AI 会话脚本和最终反馈需要直接在页面渲染，因此字段结构稍有变化，服务解析就会失败。即使再次调用同一功能，只要响应键名或数组结构发生波动，Spring DTO 反序列化和前端显示就会同时失败。':'AI conversation scripts and final feedback had to render directly on the page, so even a small field-structure change caused service parsing to fail. Even when the same feature was called again, a change in response keys or array structure could break both Spring DTO deserialization and frontend rendering.','LLM 是自然语言模型，提示词宽松时可能产生说明句、不必要的包装文本或缺失键等格式偏差。服务层假定响应是结构化 JSON，因此模型输出规范不足时，解析失败会直接变成功能失败。':'Because an LLM is a natural-language model, a loose prompt can produce format deviations such as explanations, unnecessary wrapper text, or missing keys. The service layer assumed structured JSON, so weak output rules turned parsing failures directly into feature failures.','我将提示词拆分为 Role · Instruction · Context · Output Format，明确模型必须遵守的输出契约。将响应结构限制在 JSON Schema 级别，并依据 Spring DTO 与页面所需字段收窄允许值，使服务层即使面对格式波动也能稳定接收。':'I split the prompt into Role · Instruction · Context · Output Format to make the output contract explicit. I constrained the response structure to JSON Schema level and narrowed allowed values around the fields expected by the Spring DTO and page, allowing the service layer to receive responses reliably despite format variation.','我开始优先关注 AI 响应是否具有服务可以完整消费的结构，而不是句子对人是否听起来自然。脚本生成、反馈生成和推荐后续处理共享同一响应契约后，传递到页面的失败大幅减少。':'I began prioritizing whether an AI response had a structure the service could consume end-to-end, rather than whether its sentences sounded natural. Sharing one response contract across script generation, feedback generation, and recommendation follow-up greatly reduced failures reaching the page.','外部 AI API 错误中断整个辅导':'An external AI API error interrupted the entire coaching flow','发音评估和最终反馈需要连续调用 Azure Speech 与 OpenAI，因此任何一步发生 Timeout 或解析错误，都可能让整个辅导以 500 中断。用户即使成功输入语音，也可能收不到结果页面，或因推荐阶段失败而丢失之前的反馈。':'Pronunciation assessment and final feedback called Azure Speech and OpenAI sequentially, so a Timeout or parsing error at any step could stop the entire coaching flow with 500. Even after successful voice input, users might receive no result page or lose earlier feedback when the recommendation step failed.','初始管线强烈假定外部 API 响应正常，失败时异常向上冒泡并中断整个流程。Azure 识别失败、OpenAI 响应 JSON 解析错误和推荐后续处理失败，本应是独立的局部失败，却都扩大成了整个辅导失败。':'The initial pipeline strongly assumed external API responses were normal; failures bubbled upward and interrupted the whole flow. Azure recognition failure, OpenAI response-JSON parsing errors, and recommendation follow-up failures should have been independent partial failures, but each expanded into a complete coaching failure.','我在 FastAPI 增加发音评估端点诊断日志和异常路径日志；Azure 识别失败时，改为用 STT fallback 文本和基础反馈组装响应。OpenAI 分析由 safe wrapper 包裹，失败时返回空的 problemWords 和默认消息；即使推荐阶段失败，也保留之前生成的最终反馈，并拆分 Spring 侧流程。':'I added pronunciation-assessment endpoint diagnostics and exception-path logs in FastAPI. When Azure recognition fails, the response is assembled from STT fallback text and basic feedback. OpenAI analysis is wrapped in a safe wrapper that returns empty problemWords and a default message on failure; the Spring flow is also separated so earlier final feedback is preserved even if recommendations fail.','即使部分外部 API 失败，用户也能获得最低限度的反馈和下一步信息。日志会记录失败阶段，响应则尽可能保留学习结果，从而降低整个辅导中断的频率。':'Users can still receive minimum feedback and next-step information when part of an external API fails. Logs record the failed stage while responses preserve as much learning output as possible, reducing complete coaching interruptions.','浏览器录音格式不同导致发音识别失败':'Browser recording formats caused pronunciation-recognition failures','用户直接上传浏览器录制的语音时，某些环境可以完成发音评估，另一些环境却会立即失败。用户明明已正常授予麦克风权限并完成录音，却无法识别语音。':'When users uploaded browser-recorded audio directly, pronunciation assessment worked in some environments but failed immediately in others. Users had granted microphone permission and recorded normally, yet speech could not be recognized.','不同浏览器和设备的 MediaRecorder 格式与 codec 不同，而 Azure Speech 对输入格式差异很敏感。服务假定上传语音总是同一格式，因此 WebM 文件或采样率差异会直接导致识别失败。':'MediaRecorder formats and codecs differ across browsers and devices, while Azure Speech is sensitive to input-format differences. The service assumed uploaded audio would always have one format, so WebM files or sampling differences directly caused recognition failures.','在 FastAPI 中将上传语音转换为基于 FFmpeg 的 16kHz mono WAV，统一发音评估输入。Azure 发音评估失败时返回 STT fallback 和基础反馈，即使格式有问题或输入质量较低，辅导流程也不会完全中断。':'In FastAPI, I converted uploaded audio to FFmpeg-based 16kHz mono WAV to standardize pronunciation-assessment input. When Azure pronunciation assessment fails, STT fallback and basic feedback are returned so format problems or low-quality input do not completely interrupt coaching.','浏览器间的输入差异不再直接变成服务故障；在不同录音环境下也能获得最低限度的语音评估结果。即使语音无法完美评估，用户体验也从“失败”变成了“受限结果”。':'Differences between browser inputs no longer become service failures directly, and different recording environments still produce a minimum assessment result. Even when audio cannot be assessed perfectly, the user experience changes from “failure” to a “limited result.”','React–Spring–FastAPI 语音集成错误':'React–Spring–FastAPI voice-integration error','即使用户录音在 React 中正常结束，经过 Spring 传到 FastAPI 时仍会反复出现上传失败，或响应已返回但前端播放、显示中断的问题。即使三台服务器都正常运行，边界契约稍有偏差，辅导链路也很容易断裂。':'Even when recording ended normally in React, uploads could fail while passing through Spring to FastAPI, or the frontend could stop playing or displaying a returned response. The coaching chain could break from a small boundary-contract mismatch even when all three servers were healthy.','前端在录音结束后、最终 chunk 尚未收集完成前就传递 Blob，部分请求的 File 元数据不完整。Spring 侧将 multipart 语音部分统一转换为':'The frontend passed a Blob immediately after recording ended, before collecting the final chunk, and some requests had incomplete File metadata. Spring converted the multipart audio part to','发送，丢失原始 content type 信息，导致 FastAPI 无法获得解析语音文件所需的提示。':'before sending, which lost the original content type and left FastAPI without the hint needed to interpret the audio file.','我修改 React，使其在录音结束并收集完所有 chunk 后完成 File 对象再上传，并在入口阻止空录音数据。Spring 的 FastAPI 客户端改为保留':'I changed React to complete the File object after recording ended and all chunks were collected, then upload it; empty recordings are blocked at the entry point. The Spring FastAPI client now preserves','并放入 multipart 部分发送；同时让 FastAPI 在语音评估失败时保持基础结构，稳定三台服务器之间的契约。':'and sends it in the multipart part; FastAPI also keeps a base structure when voice assessment fails, stabilizing the contract across the three servers.','从录音、上传、发音评估、反馈响应到播放的链路稳定地连成一体。实际提交中依次落实了前端 recorder 文件确认、Spring content type 保留和 FastAPI fallback 响应增强，减少了集成错误。':'The flow from recording and upload through pronunciation assessment, feedback response, and playback is now stable end-to-end. The commits successively finalized the recorder file in the frontend, preserved content type in Spring, and strengthened FastAPI fallback responses, reducing integration errors.',
  '招聘公告列表 API：平均响应时间从 15.6 秒缩短至 1.1 秒':'Job-list API: average response time reduced from 15.6s to 1.1s','不仅返回列表数据，还一次性返回 5 种筛选选项和 2 种统计结果。因此即使是未登录的单次请求也会执行 8～9 个 DB 查询；在 10 万条数据的 JMeter 压力测试中，平均响应时间达到 15,551ms、p95 为 28,654ms、TPS 仅 6.3/s、错误率为 1.11%。':'It returned not only list data but also five filter options and two statistics in one response. Even one unauthenticated request therefore executed 8–9 DB queries; in a JMeter test with 100,000 records, average latency reached 15,551ms, p95 28,654ms, TPS only 6.3/s, and error rate 1.11%.','瓶颈不在一次简单的列表查询，而在':'The bottleneck was not one simple list query, but','的 5 个 Distinct 查询，以及':'five Distinct queries from it, plus','在每次请求中重复执行的结构。此外，ACTIVE 公告查询路径没有合适的索引，很可能执行 Sequential Scan，变更频率低的聚合成本与排序成本同时累积。':'a structure that repeated them on every request. The ACTIVE job path also lacked a suitable index and was likely to use Sequential Scan, accumulating both low-change aggregation cost and sorting cost.','首先将缓存目标方法拆分到独立的 Spring Bean':'I moved cache-target methods into a separate Spring Bean','，避免因 self-invocation 导致':' to prevent self-invocation from disabling','失效。我为筛选选项设置 1 小时 TTL、为统计设置 5 分钟 TTL 并应用 Redis 缓存。由于招聘公告由 scraper 直接写入 DB，我没有使用':'。 I applied Redis caching with a one-hour TTL for filters and five minutes for statistics. Because the scraper writes jobs directly to the DB, I did not use','，改为 TTL 过期策略。同时在 PostgreSQL 中增加仅索引 ACTIVE 公告的部分索引，使实际查询目标不受 CLOSED 数据累积影响而更快命中。':'; I used TTL expiration instead. I also added a partial PostgreSQL index for ACTIVE jobs so actual query targets are faster regardless of accumulated CLOSED data.','最终，10 万条数据下平均响应时间从 15,551ms 降至 1,118ms，p95 从 28,654ms 降至 1,385ms，TPS 从 6.3/s 提升至 85.2/s。错误率也从 1.11% 降至 0.00%；根据 EXPLAIN ANALYZE，列表查询路径从 Sequential Scan 转为以 Index Scan 为主。也就是说，通过缓存减少重复聚合，并用部分索引缩小 ACTIVE 查询范围，从结构上稳定了列表 API。':'Ultimately, with 100,000 records, average latency fell from 15,551ms to 1,118ms, p95 from 28,654ms to 1,385ms, and TPS rose from 6.3/s to 85.2/s. Error rate fell from 1.11% to 0.00%; EXPLAIN ANALYZE showed the list path switching from Sequential Scan to primarily Index Scan. Caching removed repeated aggregation and a partial index narrowed ACTIVE queries, structurally stabilizing the API.','招聘公告 100 万条列表查询：后页响应时间从 84 秒缩短至 0.137 秒':'One-million job-list query: deep-page response time reduced from 84s to 0.137s','招聘公告数据增至约 100 万条后，基于 OFFSET 的列表查询越到后页越慢。尤其在 deep page，DB 丢弃前几十万行的成本高于实际用户请求，响应从几秒上升到几十秒。':'After job data grew to about one million rows, OFFSET-based list queries became slower on later pages. On deep pages, the DB cost of discarding hundreds of thousands of earlier rows exceeded the actual request cost, pushing latency from seconds into tens of seconds.','原查询先排序全部行，再跳过 OFFSET 数量的行，因此越到后页，被丢弃行的成本累积越多。此外排序相同值时没有 tie-breaker，页面边界可能重复或遗漏；如果第一阶段查询与实际组装响应之间数据发生变化，结果一致性也会动摇。':'The original query sorted all rows and then skipped OFFSET rows, so discarded-row cost accumulated on later pages. There was also no tie-breaker for equal sort values, allowing duplicates or omissions at page boundaries; changes between the first query and response assembly could also undermine consistency.','在保持 API 契约不变的前提下，先通过覆盖索引快速取出页面所需的 ID，再读取这些 ID 对应的实际行，改为 deferred join 两阶段方案。同时在所有排序中加入':'Without changing the API contract, I first used a covering index to fetch only the IDs needed for the page, then read the corresponding rows using a two-stage deferred join. I also added','tie-breaker，并在第二阶段查询中再次应用相同 predicate，确保 deep pagination 下顺序和结果一致性不受影响。':'as a tie-breaker and reapplied the same predicate in the second-stage query so order and consistency remain stable during deep pagination.','根据 EXPLAIN ANALYZE，OFFSET 50 万的位置从约 84 秒降至约 137ms，80 万的位置也稳定在约 147ms 而不再超时。不改造前端 API，仅精准移除 deep page 瓶颈，同时保住了性能与原有 UX。':'According to EXPLAIN ANALYZE, the 500,000-offset point fell from about 84s to about 137ms, and the 800,000-offset point stabilized around 147ms instead of timing out. I removed only the deep-page bottleneck without replacing the frontend API, preserving both performance and the existing UX.','订阅自动支付批处理：1 万条处理时间从 64.4 秒缩短至 17.5 秒':'Subscription auto-payment batch: 10,000-item processing reduced from 64.4s to 17.5s','自动支付批处理会随着目标订阅增加而逐条重新查询所需信息，并顺序等待 Toss 支付调用。批量变大不仅会延长完成时间，也会增加调度器推迟下一项任务的风险。':'The auto-payment batch re-queried required information one subscription at a time and waited for Toss calls sequentially. Larger batches took longer to finish and increased the risk of delaying the scheduler’s next task.','订阅、套餐、支付资料、会员信息和幂等支付信息都在循环中逐条查询，形成典型的 N+1 模式。外部支付 API 也按顺序调用，DB 瓶颈和网络等待同时拉长了 wall-clock 时间。':'Subscriptions, plans, payment profiles, member data, and idempotent-payment data were queried individually inside the loop, creating a typical N+1 pattern. External payment APIs were also called sequentially, so DB bottlenecks and network waits both extended wall-clock time.','在批处理开始前，将所需关联数据通过':'Before the batch started, I preloaded related data through','批量查询预加载到':'batch queries into','中，支付创建也复用预加载结果。在此基础上使用固定线程池进行 bounded 并行化，同时加入幂等键、单笔超时和取消处理，在提速的同时防止重复支付与调度器阻塞。':'; payment creation reused those results. I then applied bounded parallelism with a fixed thread pool, adding idempotency keys, per-payment timeouts, and cancellation handling to increase speed while preventing duplicate payments and scheduler blocking.','每笔 SQL 从 10 个降至 6 个，支付进入前的重复查询从约 3 万次缩减为实际上个位数的批量查询。1 万条的 wall-clock 从 64.4 秒降至 17.5 秒；即使 Toss 存在延迟，也从 235.6 秒降至 13.9 秒，同时保持支付准确性不变。':'SQL statements per item fell from 10 to 6, and repeated pre-payment lookups shrank from about 30,000 to effectively single-digit batch queries. For 10,000 items, wall-clock time fell from 64.4s to 17.5s; even with Toss latency it fell from 235.6s to 13.9s, while payment accuracy remained unchanged.','· 初级全栈开发者作品集':'· Junior Full-stack Developer Portfolio','GitHub 个人主页':'GitHub Profile'
};
for (const [from,to] of Object.entries(copy).sort((a,b)=>b[0].length-a[0].length)) html=html.split(from).join(to);
html=html.replace(/(<span class="case-label p">)问题(<\/span>)/g,'$1Problem$2').replace(/(<span class="case-label c">)原因(<\/span>)/g,'$1Cause$2').replace(/(<span class="case-label s">)解决方案(<\/span>)/g,'$1Solution$2').replace(/(<span class="case-label r">)结果(<\/span>)/g,'$1Result$2')
  .replace('<code>probeAuth</code>is','<code>probeAuth</code> is')
  .replace('<code>DataIntegrityViolationException</code>。','<code>DataIntegrityViolationException</code>.')
  .replace('<code>application/octet-stream</code>before','<code>application/octet-stream</code> before')
  .replace('<code>MultipartFile.getContentType()</code>and','<code>MultipartFile.getContentType()</code> and')
  .replace('<code>countActiveJobNotices()</code>、<code>countTodayNewActiveJobNotices()</code> a structure','<code>countActiveJobNotices()</code>, <code>countTodayNewActiveJobNotices()</code>, a structure')
  .replace('<code>@Cacheable</code> 。','<code>@Cacheable</code>.')
  .replace('through <code>IN</code> batch queries into <code>RenewalContext</code> ;','through <code>IN</code> batch queries into <code>RenewalContext</code>;')
  .replace('<html lang="zh-CN">','<html lang="en">').replace(/<title>.*?<\/title>/,'<title>Troubleshooting | MA RanQian Portfolio</title>').replace(/\.\.\/zh\//g,'../en/')
  .replace(/<div class="i18n-switcher"[\s\S]*?<\/div>/,'<div class="i18n-switcher" aria-label="Language"><a href="../ko/troubleshooting.html">한국어</a><a href="../zh/troubleshooting.html">中文</a><a href="../en/troubleshooting.html" aria-current="page">English</a></div>')
  .replace('</head>','<meta http-equiv="Cache-Control" content="no-store">\n</head>');
await writeFile(join(root,'en/troubleshooting.html'),html);
console.log('Generated English troubleshooting page.');
