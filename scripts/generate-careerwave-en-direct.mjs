import { readFile, writeFile } from 'node:fs/promises';
import { join } from 'node:path';
import { fileURLToPath } from 'node:url';
import { dirname } from 'node:path';

const root = join(dirname(fileURLToPath(import.meta.url)), '..');
let html = await readFile(join(root, 'zh', 'final-project.html'), 'utf8');

// Translate the reviewed Chinese page directly. This avoids chained Korean ->
// Chinese -> English replacement and keeps every source paragraph present.
const map = {
  '中文':'中文', '项目':'Projects','问题排查':'Troubleshooting','协作方式':'Collaboration','最终项目':'Final Project','迷你项目':'Mini Project',
  '最终项目 · 2026.05 – 2026.07 · 6人团队':'Final Project · 2026.05 – 2026.07 · 6-person team','AI 求职支持综合平台':'AI Career Support Platform','CareerWave 是面向 IT 求职者的 AI 个性化求职准备平台。上传简历和自我介绍后，AI 分析材料并提取核心能力，再通过实时 AI 模拟面试积累面试数据，基于这些数据推荐与当前能力匹配的职位，形成持续优化的闭环。':'CareerWave is an AI-powered, personalized career-preparation platform for IT job seekers. It analyzes resumes and self-introductions, extracts core capabilities, collects interview data through live AI mock interviews, and recommends roles matching current abilities.','CareerWave 主页面 · career-wave-rgl8bugsx-ma-ranqian.vercel.app':'CareerWave live demo · career-wave-rgl8bugsx-ma-ranqian.vercel.app','GitHub 仓库':'GitHub repository','负责范围 · 以设计为中心':'Responsibilities · design-led','负责范围':'Responsibilities','个人与企业注册 · 条款同意凭证 · 账户恢复':'Individual & company signup · consent evidence · account recovery','JWT 认证与会话管理 · 社交登录':'JWT authentication & session management · social login','订阅生命周期 · Toss 支付集成 · 管理员认证':'Subscription lifecycle · Toss payments · admin authentication','认证安全强化 · 自动支付批处理 · 招聘列表性能优化':'Authentication security · automated billing batch · job-list performance','我负责设计并实现注册、登录、找回账户、支付和维持订阅等核心流程，确保它们在真实服务中稳定衔接。':'I designed and implemented signup, login, account recovery, payment, and subscription flows so the complete user journey remains reliable.','以设计为中心':'Design-led','实现功能时，我优先考虑“六个月后是否仍能安全维护”，而不只是“现在能否运行”。在认证、支付等敏感领域，我先设计失败与异常路径，并按数据是否可丢失来划分存储位置与结构。':'I designed for safe maintenance six months later, not only immediate functionality. In authentication and payment, I modelled failure paths first and separated storage by durability requirements.','在此过程中，我用 Claude Code 和 Codex 快速比较实现前的异常场景与设计方案，并在 PR 阶段结合 CodeRabbit 和团队评审，检查边界条件与重构点。AI 不只是用来更快写代码，也用于缩短并强化设计评审与反馈闭环。':'I used Claude Code and Codex to compare exception scenarios and design alternatives, then used CodeRabbit and team review to inspect edge cases and refactoring points. AI shortened the design-review feedback loop, not just code writing.','设计产出':'Design artifacts','AWS 架构':'AWS architecture','整理用户请求经应用服务器到数据库的部署结构。':'Deployment structure from user requests through the application server to the database.','整理会员、订阅、支付、权益和条款等领域的关系、状态流转与删除策略。':'Relationships, state transitions, and deletion policies across membership, subscription, payment, entitlements, and terms.','主要实现':'Key implementation','注册与账户恢复':'Signup & account recovery','注册阶段按会员类型拆分必要校验；账户恢复则聚焦于为用户提供安全的回归路径。':'Signup validation is split by member type; account recovery provides a secure path back into the service.','拆分个人与企业注册流程':'Separate individual and company signup flows','验证企业会员营业执照编号':'Validate company registration numbers','在职证明 PDF 上传并存储到 S3':'Upload employment certificates as PDFs and store them in S3','按文档版本保存条款同意凭证':'Store consent evidence by document version','预先阻止账号、邮箱和手机号重复':'Reject duplicate accounts, emails, and phone numbers early','区分个人与企业路径的账号找回':'Separate individual and company account-recovery paths','基于 Reset Token 哈希存储的密码重置':'Password reset with hashed reset tokens','包含 TTL 与失败次数限制的恢复防护逻辑':'Recovery safeguards with TTL and attempt limits','认证、会话与社交登录':'Authentication, sessions & social login','相比登录成功本身，我更关注登录后的会话如何维持，以及社交账号如何稳定关联既有会员信息。':'I focused on maintaining sessions after login and reliably linking social accounts to existing member records.','Access Token · Refresh Token 双令牌结构':'Access Token · Refresh Token dual-token structure','基于 Redis JTI 黑名单的退出即刻失效':'Immediate logout invalidation with a Redis JTI blacklist','仅存储 Refresh Token 的 SHA-256 哈希':'Store only SHA-256 hashes of refresh tokens','每位用户最多同时 5 个会话':'Limit each user to five concurrent sessions','应用会话空闲超时':'Session idle timeout','通过登录 IP Rate Limit 控制重复尝试':'Control repeated attempts with login IP rate limits','集成 Kakao、Naver、Google 三方 OAuth':'Integrate Kakao, Naver, and Google OAuth','社交注册时拆分补充信息步骤':'Separate the additional-information step for social signup','关联既有会员账户并提示上次登录方式':'Link existing accounts and show the last login method','为应对 CWE-598 切换为短期 Cookie 传递':'Use short-lived cookies to address CWE-598','订阅与支付一致性':'Subscription & payment consistency','将订阅生命周期与支付结算纳入同一流程，避免支付成功后订阅状态、权益和用量彼此不一致。':'I handled subscription lifecycle and payment settlement as one flow so subscription status, entitlements, and usage remain consistent after payment.','覆盖套餐选择、激活、取消和到期的订阅生命周期':'Subscription lifecycle from plan selection to activation, cancellation, and expiration','按产品发放并管理一次性免费权益':'Issue and manage one-time free entitlements per product','提供月度用量、使用率和订阅状态查询':'Expose monthly usage, utilization, and subscription status','以单事务处理支付、订阅、权益和用量变更':'Process payment, subscription, entitlement, and usage changes in one transaction','以 REQUIRES_NEW 独立保存 Toss 失败记录':'Persist Toss failure records independently with REQUIRES_NEW','基于 orderId、idempotencyKey、paymentKey 防止重复支付':'Prevent duplicate payments with orderId, idempotencyKey, and paymentKey','AES 加密存储 billingKey，并从序列化路径中排除':'Encrypt billingKey with AES and exclude it from serialization paths','代表界面':'Key screens','个人注册':'Individual signup','企业注册 · 在职证明':'Company signup · employment certificate','上次登录方式提示':'Last login method','找回账户':'Account recovery','找回个人账号':'Find individual account','找回企业账号':'Find company account','订阅支付':'Subscription payment','订阅状态':'Subscription status','订阅与支付记录':'Subscription & payment history','核心成果':'Key outcomes','大数据量与批处理性能优化':'Large-scale and batch performance','约 94% ↓':'~94% ↓','自动支付批处理优化':'Automated billing batch optimization','通过批量预加载和基于幂等键的并行化，改善 N+1 查询与串行调用瓶颈；支付准确率保持 100%。':'Improved N+1 queries and serial-call bottlenecks with batch preloading and idempotency-key parallelization; payment accuracy stayed at 100%.','约 99.8% ↓':'~99.8% ↓','招聘列表深度分页':'Deep pagination for job listings','通过覆盖索引与 deferred join 改善百万级 OFFSET 瓶颈，同时保持 API 与前端契约不变。':'Improved the million-row OFFSET bottleneck with a covering index and deferred join while preserving the API/frontend contract.','约 93% ↓':'~93% ↓','招聘列表 API':'Job-list API','将重复聚合查询拆分为 Redis 缓存，并应用仅包含 ACTIVE 职位的 PostgreSQL 部分索引。':'Moved repeated aggregation queries to Redis cache and added a PostgreSQL partial index containing only ACTIVE jobs.','认证与社交登录安全强化':'Authentication & social-login security','JWT · 会话安全基础设施':'JWT · session security infrastructure','退出即刻失效 · 限制 5 个会话':'Immediate logout invalidation · five-session limit','通过 Redis JTI 黑名单让令牌退出即刻失效，仅存储 Refresh Token 的 SHA-256 哈希；将登录失败计数（Redis）与锁定状态（DB）分开保存。':'The Redis JTI blacklist invalidates tokens immediately on logout; only refresh-token SHA-256 hashes are stored, while failed-login counts (Redis) and lock state (DB) are separated.','社交登录安全强化':'Social-login security hardening','阻断 CWE-598 · 60 秒短期 Cookie':'CWE-598 blocked · 60-second short-lived cookie','将社交回调中 access token 暴露在 URL 的漏洞（CWE-598）改为 60 秒、SameSite=Strict 短期 Cookie，阻断 3 条暴露路径；state 通过 getAndDelete 原子处理防止重复回调。':'I replaced URL-exposed social-callback access tokens (CWE-598) with 60-second SameSite=Strict cookies, blocking three exposure paths; atomic getAndDelete handling prevents duplicate callbacks.','技术决策':'Technical decisions','核心能力':'Core capabilities','可用性 vs 安全性':'Availability vs security','Redis 故障时按角色区分策略':'Role-based policy during Redis failures','黑名单查询失败时可以阻断所有用户，但这会导致大量普通用户被强制退出；全部放行则可能让高权限账户绕过安全检查，风险同样很高。':'Blocking everyone when blacklist lookup fails could force ordinary users out at scale; allowing everyone could let privileged accounts bypass security.','因此，普通用户优先保证可用性而放行，管理员则优先保证安全性而拒绝。':'I therefore allow ordinary users for availability, but deny administrators for security.','这个决定基于一个原则：即使是同一种故障，也不必对所有用户采用相同策略。':'The principle: the same failure does not require the same policy for every user.','Redis（临时）+ DB（权威）':'Redis (temporary) + DB (authoritative)','分开保存登录失败次数与账户锁定状态':'Store login-failure counts and account-lock state separately','便利性 vs 暴露风险':'Convenience vs exposure risk','billingKey 不仅加密保存，还从序列化路径中结构性排除':'Encrypt billingKey and structurally exclude it from serialization paths','自动实现 vs 复杂查询拆分':'Generated queries vs complex-query separation','分离 JPA Repository 与 QueryRepository 职责':'Separate JPA Repository and QueryRepository responsibilities','深度分页性能问题':'Deep-pagination performance issue','社交登录用户支付 404 问题':'404 payment issue for social-login users','招聘数据扩展到 100 万条后，列表 API 在深分页区间响应急剧变慢。起初我误以为负载测试中的延迟是缓存问题，实际瓶颈来自 OFFSET 本身。通过 EXPLAIN ANALYZE 重新确认执行计划后，应用 ACTIVE 职位覆盖索引与 deferred join 解决了问题。':'After expanding job data to one million rows, the listing API slowed sharply on deep pages. I initially suspected cache synchronization, but OFFSET itself was the bottleneck. EXPLAIN ANALYZE confirmed the plan; a covering index for ACTIVE jobs and deferred join solved the issue.','另一个案例是 Kakao 登录用户发起支付时才出现 404。直接查询数据库发现部分社交登录用户邮箱为空，订单创建时的 NOT NULL 约束异常被错误地吸收到重复请求 catch 逻辑中。随后增加 INSERT 前校验、拆分异常处理，并同步修正前端状态码分支。':'Another issue was a 404 that occurred only when Kakao-login users attempted payment. A database check found missing emails for some social-login users, while a NOT NULL violation was incorrectly absorbed by duplicate-request handling. I added pre-insert validation, separated exception handling, and fixed the frontend status branch.','协作':'Collaboration','先通过文档对齐需求与职责，并在实现前记录变更，让团队依据同一标准。':'I align requirements and ownership in documentation first, record changes before implementation, and keep the team working from one shared standard.','与前端共同核对 Spec、Swagger、DTO 和 TypeScript 类型以对齐 API 契约，并在认证与计费领域统一使用 ApiResponse，减少返工。':'I align the API contract with the frontend through Spec, Swagger, DTOs, and TypeScript types, and use ApiResponse across authentication and billing to reduce rework.','QA 发现的问题不只从后端单独排查，而是结合数据库、日志和前端行为逐步缩小根因。':'For QA issues, I inspect the database, logs, and frontend behavior together to narrow the root cause.','项目收获':'Lessons learned','通过这个项目，我比单纯实现功能更清晰地建立了稳定运营服务的设计标准。':'This project gave me clearer design standards for operating reliable services, beyond simply implementing features.','我学到性能与稳定性应以日志、执行计划、响应时间等可复现指标验证，而不是凭感觉判断。':'I learned to validate performance and stability with reproducible metrics such as logs, execution plans, and response times.','我也切身体会到，越是包含外部支付商或社交登录等不可控因素，越要在早期设计中同时考虑 fallback 与异常处理。':'The less controllable an integration is, such as payments or social login, the earlier fallback and exception handling must be designed.','尤其在认证和支付等敏感领域，不能只实现正常流程，还必须确保失败时服务链路不会中断。':'In sensitive domains such as authentication and payments, the service flow must remain intact even when the happy path fails.','代码与 PR':'Code & PR','修复社交登录支付 404':'Fix social-login payment 404','优化招聘列表 API 性能':'Optimize job-list API performance','会话空闲超时 · 多层防护':'Session idle timeout · layered defense','改善招聘列表深度分页':'Improve deep pagination for job listings','概览·职责':'Overview · responsibilities','协作·复盘':'Collaboration · retrospective','代码·PR':'Code · PR','GitHub 个人主页':'GitHub profile','· 全栈初级开发者作品集':'· Junior full-stack developer portfolio'
};

for (const [from, to] of Object.entries(map).sort((a, b) => b[0].length - a[0].length)) html = html.split(from).join(to);
const residual = {
  '没有把登录失败相关数据全部放进一个存储，而是按数据性质拆分。失败次数应通过 TTL 自动过期且写入频繁，因此放在 Redis；账户锁定状态即使服务器重启或 Redis 故障也必须保留，所以存入数据库。':'I separated login-failure data by purpose: attempt counts belong in Redis because they expire by TTL, while account-lock state belongs in the database because it must survive restarts and Redis outages.',
  '这样即使 Redis 不可用，已锁定账户仍保持锁定；而失败次数这类临时数据可以快速累积并自动清理。':'This keeps locked accounts protected when Redis is unavailable, while temporary attempt counts can be accumulated and cleaned up quickly.',
  '这不是按技术栈划分存储，而是按“该状态能否丢失、是否必须保留”来决定。':'The storage split is based on whether a state may disappear or must be retained, not on technology preference.',
  'billingKey 是一旦暴露就可能导致未授权支付的高敏感支付信息。仅以 AES 加密保存并不足够，真实服务中仍可能发生已加密值被误序列化或混入日志的事故。':'billingKey is highly sensitive payment data: exposure could enable unauthorized charges. AES encryption alone is insufficient because encrypted values can still be accidentally serialized or logged.',
  '因此在保存阶段使用 AES 加密，并通过 @Getter(AccessLevel.NONE) 与专用访问方法，让它从 Jackson 的序列化候选中结构性排除。':'I applied AES encryption at rest and used @Getter(AccessLevel.NONE) with a dedicated accessor to exclude billingKey from Jackson serialization candidates.',
  '这个决定的核心是：敏感信息不仅要妥善保存，还要减少任何可能因误操作泄露到外部的路径。':'The key principle is to protect sensitive data at rest and minimize every path through which it could leak accidentally.',
  '查询逻辑没有全部采用同一种方式。简单重复检查和单条查询使用 JPA Repository 方法；涉及多表和复杂条件的查询则拆分到独立 QueryRepository。':'I use JPA Repository methods for simple checks and lookups, and a dedicated QueryRepository for multi-table, complex queries.',
  '如果把简单查询也全部塞进复杂查询层，代码会过重；如果把复杂连接硬塞进基于方法命名的 Repository，查询意图会模糊且难以维护。':'A complex query layer would make simple reads too heavy, while method-name repositories would hide complex join intent and hurt maintainability.',
  '这不是强制统一，而是按查询复杂度划分存储层职责。':'This is a complexity-based separation of responsibilities, not forced uniformity.',
  '这段经历让我养成了不凭体感或负载测试噪声判断，而以可复现指标定位瓶颈的习惯。':'This experience taught me to locate bottlenecks with reproducible metrics instead of intuition or load-test noise.',
  '这个过程让我认识到，表面相同的异常可能有不同根因；数据约束违反应优先通过前置校验阻断。':'I learned that similar-looking errors can have different causes, and that data-constraint violations should be stopped with pre-validation.',
  '查看认证、支付与性能排障完整记录 →':'View the complete authentication, payment, and performance troubleshooting record →',
  'Collaboration与Lessons learned':'Collaboration & lessons learned',
  '先通过文档对齐需求与职责，并在实现前记录变更，让团队依据同一标准Collaboration。':'I align requirements and ownership in documentation first, record changes before implementation, and keep the team working from one shared standard.',
  '我将 Issue 对应分支与 PR 说明关联，记录变更意图与范围；把 CodeRabbit 和团队评审反馈落实为修正提交，保持讨论到代码的可追踪性。':'I link issue branches to PR descriptions, record intent and scope, and apply CodeRabbit and team-review feedback as follow-up commits so decisions remain traceable in code.',
  '查看分支策略、提交、PR 与评审Collaboration →':'View branch, commit, PR, and review collaboration →',
  '查看 ixxveon/career-wave 仓库':'View ixxveon/career-wave repository',
  '아키텍처':'architecture', '다이어그램':'diagram', '메인 화면':'main screen',
  '개요':'Overview', '역할':'Role', '문제':'Issues', '화면':'screen',
  '기업 회원가입 화면':'company signup screen', '내역 화면':'history screen',
  '닫기':'Close', '확대 이미지':'Enlarged image'
};
for (const [from, to] of Object.entries(residual).sort((a, b) => b[0].length - a[0].length)) html = html.split(from).join(to);
html = html.replace('<html lang="zh-CN">', '<html lang="en">')
  .replace(/\.\.\/zh\//g, '../en/')
  .replace(/<div class="i18n-switcher"[\s\S]*?<\/div>/, '<div class="i18n-switcher" aria-label="Language"><a href="../ko/final-project.html">한국어</a><a href="../zh/final-project.html">中文</a><a href="../en/final-project.html" aria-current="page">English</a></div>')
  .replace(/<title>.*?<\/title>/, '<title>CareerWave Final Project</title>')
  // Compact labels keep the TOC border in the same visual position as the
  // Korean and Chinese pages instead of widening leftward for English copy.
  .replace(/<nav class="toc"[\s\S]*?<\/nav>/, `<nav class="toc" aria-label="Table of Contents">
    <a href="#about">Overview · Role</a>
    <a href="#design">Design</a>
    <a href="#build">Implementation</a>
    <a href="#screens">Screens</a>
    <a href="#result">Outcomes</a>
    <a href="#decisions">Decisions</a>
    <a href="#trouble">Issues</a>
    <a href="#collab">Collaboration</a>
    <a href="#code">Code · PR</a>
</nav>`)
  .replace('</head>', '<meta http-equiv="Cache-Control" content="no-store">\n</head>');
await writeFile(join(root, 'en', 'final-project.html'), html);
console.log('Generated English CareerWave page from the reviewed Chinese source.');
