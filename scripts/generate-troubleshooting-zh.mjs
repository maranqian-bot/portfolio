import { readFile, writeFile } from 'node:fs/promises';
import { join } from 'node:path';

const root = join(new URL('..', import.meta.url).pathname);
let html = await readFile(join(root, 'ko/troubleshooting.html'), 'utf8');

const copy = {
  '트러블슈팅 | 마은재 포트폴리오':'问题排查 | MA RanQian 作品集','About me':'关于我','프로젝트':'项目','파이널 프로젝트':'最终项目','미니 프로젝트':'迷你项目','트러블슈팅':'问题排查','협업방식':'协作方式','한국어':'한국어','문제를 재현하고 원인을 좁힌 기록':'复现问题并逐步缩小原因的记录','Problem Solving':'问题排查与解决','Mapingo의 AI 회화 코칭과 CareerWave의 인증·결제 도메인을 담당하며 만난 문제를 재현 조건, 원인 분석, 해결, 재검증으로 나누어 기록했습니다.':'我将负责 Mapingo AI 会话辅导与 CareerWave 认证、支付领域时遇到的问题，按复现条件、原因分析、解决方案和重新验证进行记录。','전체':'全部','Performance':'性能','Billing':'支付','AI · LLM':'AI · LLM','Speech':'语音',
  '파이널 프로젝트 · CareerWave':'最终项目 · CareerWave','미니 프로젝트 · Mapingo':'迷你项目 · Mapingo',
  'JWT 인증 필터 예외가 500으로 반환되는 문제':'JWT 认证过滤器异常被返回为 500','만료되었거나 위변조된 토큰으로 보호된 API를 호출했을 때 클라이언트가 기대한 401 응답이 아니라 500 응답이 반환되었습니다. 프론트엔드에서는 로그인 만료와 서버 오류를 구분할 수 없어 재로그인 안내 흐름이 깨졌습니다.':'调用受过期或篡改令牌保护的 API 时，返回的不是客户端预期的 401，而是 500。前端无法区分登录过期和服务器错误，重新登录提示流程因此失效。','처음에는 전역 예외 처리 설정 문제로 보고 Controller Advice를 확인했지만, JWT 검증은 DispatcherServlet에 도달하기 전 Security Filter Chain에서 실행되고 있었습니다. 따라서 필터 내부에서 발생한 토큰 만료, 서명 불일치 예외는 Controller Advice가 잡을 수 없었고, 별도 처리 없이 RuntimeException처럼 흘러가며 500으로 변환되었습니다.':'起初我以为是全局异常处理配置的问题并检查了 Controller Advice，但 JWT 验证实际上在到达 DispatcherServlet 前的 Security Filter Chain 中执行。因此过滤器内部产生的令牌过期、签名不匹配异常无法被 Controller Advice 捕获，在没有单独处理的情况下像 RuntimeException 一样继续传播，最终转换成 500。','JWT 필터에서는 토큰 검증 실패 원인을 AuthenticationException 계열로 정리하고, AuthenticationEntryPoint에서 401 상태 코드와 에러 메시지를 명시적으로 반환하도록 처리했습니다. 케이스별 메시지가 과하게 노출되지 않도록 클라이언트 안내용 메시지와 서버 로그를 분리했습니다.':'在 JWT 过滤器中，我将令牌验证失败原因整理为 AuthenticationException 类型，并在 AuthenticationEntryPoint 中明确返回 401 状态码和错误消息。为避免暴露过多细节，我分离了面向客户端的提示消息与服务器日志。','토큰 누락, 만료, 위변조 요청을 각각 재현해 모두 401로 응답되는지 확인했습니다. 정상 토큰 요청은 기존 API 흐름을 유지했고, 프론트엔드는 401을 기준으로 재로그인 안내를 처리할 수 있게 되었습니다.':'我分别复现令牌缺失、过期和篡改请求，确认它们全部返回 401。正常令牌请求保持原有 API 流程，前端也可以依据 401 处理重新登录提示。',
  '새로고침 시 동시 refresh 경쟁으로 로그아웃':'刷新时因 refresh 竞争而退出登录','페이지 새로고침이나 OAuth 콜백 직후처럼 access token이 아직 메모리에 복구되지 않은 순간, 여러 컴포넌트가 동시에 세션 복원을 시도하면서 사용자가 바로 로그아웃되는 문제가 있었습니다. 정상적인 refresh 쿠키가 있어도 화면 진입 타이밍에 따라 세션이 끊겨 UX가 불안정했습니다.':'在页面刷新或 OAuth 回调后、access token 尚未恢复到内存的瞬间，多个组件同时尝试恢复会话，导致用户立即退出登录。即使存在有效的 refresh Cookie，会话也会因进入页面的时机而中断，造成不稳定的体验。','401 응답이나 초기 보호 라우트 진입 시 각 요청이 독립적으로':'在收到 401 响应或进入初始受保护路由时，每个请求都会独立地','를 호출하고 있었고, 서버는 refresh token rotation과 재사용 탐지를 적용하고 있었습니다. 같은 쿠키로 거의 동시에 두 번 이상 재발급이 들어오면 먼저 회전된 토큰이 뒤늦게 다시 사용된 것으로 판단되어 세션 전체가 폐기될 수 있었습니다.':'调用，服务器同时启用了 refresh token rotation 和重复使用检测。如果使用同一个 Cookie 几乎同时发起两次以上重新签发，系统可能判断已轮换的令牌被再次使用，从而废弃整个会话。','프론트엔드의 세션 복원 로직을 single-flight로 묶어 진행 중인 refresh Promise에 후속 요청이 합류하도록 바꿨습니다. 또한 OAuth 콜백 페이지에서':'我将前端会话恢复逻辑合并为 single-flight，让后续请求加入正在进行的 refresh Promise。同时在 OAuth 回调页面通过','로 refresh 쿠키 회전이 끝나기 전에 이동하지 않도록 순서를 고정해, 새 페이지가 구 쿠키로 다시 refresh를 발사하는 경쟁 창도 닫았습니다.':'固定顺序，确保 refresh Cookie 轮换完成后再跳转，关闭新页面使用旧 Cookie 再次发起 refresh 的竞争窗口。','를 동시에 여러 번 호출해도 refresh 요청이 1회만 나가는 테스트로 재현 케이스를 고정했습니다. OAuth 콜백 이후에도 회전 완료 전에는 이동하지 않도록 검증해, 새로고침 직후 즉시 로그아웃되던 현상이 사라졌습니다.':'即使同时多次调用，也通过测试固定为只发出一次 refresh 请求。OAuth 回调后同样验证轮换完成前不跳转，刷新后立即退出登录的现象消失。',
  '소셜 로그인 사용자 결제 시 404':'社交登录用户支付时返回 404','카카오 소셜 로그인 사용자가 결제를 시도할 때, 프론트엔드에서는 결제 페이지 진입 대신 404 오류가 떨어지는 문제가 QA에서 발견됐습니다. 사용자는 결제 준비 실패 원인을 알 수 없었고, 백엔드도 실제 원인과 다른 예외 경로를 타고 있었습니다.':'QA 发现，Kakao 社交登录用户尝试支付时，前端没有进入支付页面而是收到 404。用户无法知道支付准备失败的原因，后端也走上了与实际原因不符的异常路径。','일부 소셜 계정은 이메일이 비어 있었고, 이 값이':'部分社交账号的邮箱为空，该值违反了','제약에 걸리면서 DB 저장 단계에서':'约束，并在 DB 保存阶段产生了','이 발생했습니다. 그런데 이 예외가 원래는 동시 요청 충돌을 복구하기 위한 catch 블록으로 흘러들어가면서, 실제 제약 위반이 주문 없음처럼 처리되어 404로 바뀌고 있었습니다.':'。但该异常流入原本用于恢复并发请求冲突的 catch 代码块，实际的约束违反被当成订单不存在处理，最终变成了 404。','결제 INSERT 이전에 이메일 존재 여부를 명시적으로 검증해 제약 위반을 비즈니스 오류로 앞단에서 막았습니다. 동시에 프론트엔드 오류 분기와 메시지를 수정해, 결제 진입 실패가 404가 아니라 사용자 정보 보완이 필요한 케이스로 처리되도록 정리했습니다.':'我在支付 INSERT 前明确验证邮箱是否存在，在前端阻止约束违反并转换为业务错误。同时修改前端错误分支和消息，将支付进入失败处理为需要补充用户信息，而不是 404。','이메일이 없는 소셜 계정으로 결제 경로를 다시 재현했을 때 더 이상 404로 오인되지 않았습니다. QA 단계에서 원인 확인에 쓰였던 DB 직접 조회 결과와 응답 흐름이 일치하게 바뀌었고, 사용자도 결제 실패 원인을 올바르게 안내받게 되었습니다.':'使用没有邮箱的社交账号重新复现支付路径时，不再被误判为 404。QA 阶段用于确认原因的 DB 直接查询结果与响应流程保持一致，用户也能收到正确的支付失败原因。',
  'LLM 응답 형식이 바뀌면 파싱 실패':'LLM 响应格式变化导致解析失败','AI 회화 스크립트와 최종 피드백은 화면에서 바로 렌더링해야 했기 때문에 필드 구조가 조금만 달라져도 서비스 파싱이 깨졌습니다. 같은 기능을 다시 호출해도 응답 키 이름이나 배열 구조가 흔들리면 Spring DTO 역직렬화와 프론트 표시가 함께 실패했습니다.':'AI 会话脚本和最终反馈需要直接在页面渲染，因此字段结构稍有变化，服务解析就会失败。即使再次调用同一功能，只要响应键名或数组结构发生波动，Spring DTO 反序列化和前端显示就会同时失败。','LLM은 자연어 모델이라 프롬프트가 느슨하면 설명 문장, 불필요한 래핑 텍스트, 키 누락처럼 형식 편차를 만들 수 있습니다. 서비스 계층은 이 응답을 구조화된 JSON으로 가정하고 있었기 때문에, 모델 출력 규약이 약한 상태에서는 파싱 실패가 곧 사용자 기능 실패로 이어졌습니다.':'LLM 是自然语言模型，提示词宽松时可能产生说明句、不必要的包装文本或缺失键等格式偏差。服务层假定响应是结构化 JSON，因此模型输出规范不足时，解析失败会直接变成功能失败。','프롬프트를 Role·Instruction·Context·Output Format으로 분리해 모델이 따라야 할 출력 계약을 더 명확히 했습니다. JSON Schema 수준으로 응답 구조를 제한하고, Spring DTO와 화면이 기대하는 필드를 기준으로 허용값을 좁혀 응답 형식이 흔들려도 서비스 계층이 안정적으로 받을 수 있게 정리했습니다.':'我将提示词拆分为 Role · Instruction · Context · Output Format，明确模型必须遵守的输出契约。将响应结构限制在 JSON Schema 级别，并依据 Spring DTO 与页面所需字段收窄允许值，使服务层即使面对格式波动也能稳定接收。','AI 응답이 사람에게 그럴듯한 문장인지보다 서비스가 끝까지 소비할 수 있는 구조인지에 초점을 맞추게 되었습니다. 스크립트 생성, 피드백 생성, 추천 후속 처리까지 같은 응답 계약을 공유하게 되면서 화면 전달 실패가 크게 줄었습니다.':'我开始优先关注 AI 响应是否具有服务可以完整消费的结构，而不是句子对人是否听起来自然。脚本生成、反馈生成和推荐后续处理共享同一响应契约后，传递到页面的失败大幅减少。',
  '외부 AI API 오류가 전체 코칭을 중단':'外部 AI API 错误中断整个辅导','발음 평가와 최종 피드백은 Azure Speech와 OpenAI를 연달아 호출하는 구조였기 때문에, 한 단계라도 Timeout이나 파싱 오류가 나면 코칭 전체가 500으로 끊길 수 있었습니다. 사용자는 음성 입력에 성공해도 결과 화면을 받지 못하거나, 추천 단계 실패 때문에 앞선 피드백까지 잃는 상황이 생겼습니다.':'发音评估和最终反馈需要连续调用 Azure Speech 与 OpenAI，因此任何一步发生 Timeout 或解析错误，都可能让整个辅导以 500 中断。用户即使成功输入语音，也可能收不到结果页面，或因推荐阶段失败而丢失之前的反馈。','초기 파이프라인은 외부 API 응답이 정상이라는 가정이 강했고, 실패 시 예외가 상위까지 전파되어 전체 흐름을 중단시키고 있었습니다. 특히 Azure 인식 실패, OpenAI 응답 JSON 파싱 오류, 추천 후속 처리 실패가 각각 독립적인 부분 실패가 아니라 전체 코칭 실패로 확대되고 있었습니다.':'初始管线强烈假定外部 API 响应正常，失败时异常向上冒泡并中断整个流程。Azure 识别失败、OpenAI 响应 JSON 解析错误和推荐后续处理失败，本应是独立的局部失败，却都扩大成了整个辅导失败。','FastAPI 쪽에 발음 평가 엔드포인트 진단 로그와 예외 경로 로그를 추가하고, Azure 인식이 실패하면 STT fallback 텍스트와 기본 피드백으로 응답을 조립하도록 바꿨습니다. OpenAI 분석은 safe wrapper로 감싸 실패 시 빈 problemWords와 기본 메시지를 반환하게 했고, 추천 단계가 실패해도 앞서 생성된 최종 피드백은 보존하도록 Spring 쪽 흐름도 분리했습니다.':'我在 FastAPI 增加发音评估端点诊断日志和异常路径日志；Azure 识别失败时，改为用 STT fallback 文本和基础反馈组装响应。OpenAI 分析由 safe wrapper 包裹，失败时返回空的 problemWords 和默认消息；即使推荐阶段失败，也保留之前生成的最终反馈，并拆分 Spring 侧流程。','외부 API 일부가 실패해도 사용자는 최소한의 피드백과 다음 단계 정보를 받을 수 있게 되었습니다. 로그에는 어느 단계에서 실패했는지 남기되, 응답은 가능한 범위의 학습 결과를 유지하도록 바뀌어 코칭 전체 중단 빈도를 낮췄습니다.':'即使部分外部 API 失败，用户也能获得最低限度的反馈和下一步信息。日志会记录失败阶段，响应则尽可能保留学习结果，从而降低整个辅导中断的频率。',
  '브라우저마다 녹음 포맷이 달라 발음 인식 실패':'浏览器录音格式不同导致发音识别失败','사용자가 브라우저에서 녹음한 음성을 그대로 업로드하면 어떤 환경에서는 발음 평가가 되지만, 어떤 환경에서는 같은 기능이 바로 실패했습니다. 사용자는 마이크 권한과 녹음은 정상인데도 음성 인식이 안 되는 상황을 겪었습니다.':'用户直接上传浏览器录制的语音时，某些环境可以完成发音评估，另一些环境却会立即失败。用户明明已正常授予麦克风权限并完成录音，却无法识别语音。','브라우저와 기기마다 MediaRecorder가 만드는 포맷과 codec이 달랐고, Azure Speech는 입력 포맷 편차에 민감했습니다. 서비스는 업로드된 음성을 동일한 형식으로 들어올 것처럼 가정하고 있었기 때문에, WebM 계열 파일이나 샘플링 차이가 그대로 인식 실패로 이어졌습니다.':'不同浏览器和设备的 MediaRecorder 格式与 codec 不同，而 Azure Speech 对输入格式差异很敏感。服务假定上传语音总是同一格式，因此 WebM 文件或采样率差异会直接导致识别失败。','FastAPI에서 업로드 음성을 FFmpeg 기반 16kHz mono WAV로 변환해 발음 평가 입력을 표준화했습니다. Azure 발음 평가가 실패할 때는 STT fallback과 기본 피드백을 반환하도록 해, 포맷 문제나 낮은 품질 입력에서도 코칭 흐름이 완전히 끊기지 않도록 보완했습니다.':'在 FastAPI 中将上传语音转换为基于 FFmpeg 的 16kHz mono WAV，统一发音评估输入。Azure 发音评估失败时返回 STT fallback 和基础反馈，即使格式有问题或输入质量较低，辅导流程也不会完全中断。','브라우저별 입력 편차가 서비스 장애로 바로 이어지지 않게 되었고, 서로 다른 녹음 환경에서도 최소한의 음성 평가 결과를 받을 수 있게 되었습니다. 음성이 완벽히 평가되지 않더라도 사용자 경험은 “실패”가 아니라 “제한된 결과”로 바뀌었습니다.':'浏览器间的输入差异不再直接变成服务故障；在不同录音环境下也能获得最低限度的语音评估结果。即使语音无法完美评估，用户体验也从“失败”变成了“受限结果”。',
  'React–Spring–FastAPI 음성 연동 오류':'React–Spring–FastAPI 语音集成错误','사용자 녹음이 React에서 정상적으로 끝나도 Spring을 거쳐 FastAPI로 전달되는 과정에서 업로드가 실패하거나, 응답은 왔는데 프론트에서 재생/표시가 끊기는 문제가 반복됐습니다. 세 서버가 모두 살아 있어도 경계면 계약이 조금만 어긋나면 코칭 체인이 쉽게 깨졌습니다.':'即使用户录音在 React 中正常结束，经过 Spring 传到 FastAPI 时仍会反复出现上传失败，或响应已返回但前端播放、显示中断的问题。即使三台服务器都正常运行，边界契约稍有偏差，辅导链路也很容易断裂。','프론트에서는 녹음 종료 직후 아직 최종 chunk 수집이 끝나기 전에 Blob을 넘기고 있었고, 일부 요청은 File 메타데이터가 불완전한 상태였습니다. Spring 쪽에서는 multipart 음성 파트를 일괄':'前端在录音结束后、最终 chunk 尚未收集完成前就传递 Blob，部分请求的 File 元数据不完整。Spring 侧将 multipart 语音部分统一转换为','으로 바꿔 보내고 있어 원래의 content type 정보가 사라졌고, 그 결과 FastAPI가 음성 파일을 해석하는 데 필요한 힌트를 잃고 있었습니다.':'发送，丢失原始 content type 信息，导致 FastAPI 无法获得解析语音文件所需的提示。','React에서는 녹음 종료 후 최종 chunk를 모두 모은 뒤 File 객체를 완성해 업로드하도록 수정하고, 빈 녹음 데이터는 앞단에서 차단했습니다. Spring의 FastAPI 연동 클라이언트에서는':'我修改 React，使其在录音结束并收集完所有 chunk 后完成 File 对象再上传，并在入口阻止空录音数据。Spring 的 FastAPI 客户端改为保留','을 보존해 multipart 파트에 실어 보내도록 바꿨고, FastAPI 응답도 음성 평가 실패 시 기본 구조를 유지하도록 정리해 세 서버 간 계약을 안정화했습니다.':'并放入 multipart 部分发送；同时让 FastAPI 在语音评估失败时保持基础结构，稳定三台服务器之间的契约。','녹음부터 업로드, 발음 평가, 피드백 응답, 재생까지의 흐름이 한 번에 이어지도록 안정성이 높아졌습니다. 실제 커밋에서는 프론트의 recorder 파일 확정, Spring의 content type 보존, FastAPI의 fallback 응답 보강이 순차적으로 반영되며 연동 오류를 줄였습니다.':'从录音、上传、发音评估、反馈响应到播放的链路稳定地连成一体。实际提交中依次落实了前端 recorder 文件确认、Spring content type 保留和 FastAPI fallback 响应增强，减少了集成错误。',
  '채용 공고 목록 API, 평균 응답 시간을 15.6초에서 1.1초로 단축':'招聘公告列表 API：平均响应时间从 15.6 秒缩短至 1.1 秒','는 목록 데이터뿐 아니라 필터 옵션 5종과 통계 2종까지 한 번에 내려주고 있었습니다. 그 결과 비로그인 기준 단일 요청에서도 DB 쿼리가 8~9개 실행됐고, 10만 건 데이터 기준 JMeter 부하 테스트에서 평균 응답시간 15,551ms, p95 28,654ms, TPS 6.3/s, 에러율 1.11%까지 떨어졌습니다.':'不仅返回列表数据，还一次性返回 5 种筛选选项和 2 种统计结果。因此即使是未登录的单次请求也会执行 8～9 个 DB 查询；在 10 万条数据的 JMeter 压力测试中，平均响应时间达到 15,551ms、p95 为 28,654ms、TPS 仅 6.3/s、错误率为 1.11%。','병목은 단순 목록 조회 1건이 아니라,':'瓶颈不在一次简单的列表查询，而在','의 Distinct 쿼리 5개와':'的 5 个 Distinct 查询，以及','가 요청마다 반복 실행되는 구조에 있었습니다. 여기에 ACTIVE 공고 조회 경로도 적절한 인덱스 없이 Sequential Scan 가능성이 큰 상태라, 변경 빈도가 낮은 집계와 정렬 비용이 동시에 누적되고 있었습니다.':'在每次请求中重复执行的结构。此外，ACTIVE 公告查询路径没有合适的索引，很可能执行 Sequential Scan，变更频率低的聚合成本与排序成本同时累积。','먼저 캐시 대상 메서드를 별도 Spring Bean인':'首先将缓存目标方法拆分到独立的 Spring Bean','로 분리해 self-invocation 때문에':'，避免因 self-invocation 导致','이 무력화되지 않도록 구조를 바꿨습니다. 필터 옵션은 TTL 1시간, 통계는 TTL 5분으로 Redis 캐시를 적용했고, 채용 공고가 스크래퍼에 의해 DB에 직접 삽입되는 구조라':'失效。我为筛选选项设置 1 小时 TTL、为统计设置 5 分钟 TTL 并应用 Redis 缓存。由于招聘公告由 scraper 直接写入 DB，','대신 TTL 만료 전략을 택했습니다. 동시에 PostgreSQL에는 ACTIVE 공고만 인덱싱하는 부분 인덱스를 추가해, CLOSED 데이터 누적과 무관하게 실제 조회 대상만 더 빠르게 타도록 정리했습니다.':'我没有使用 ，改为 TTL 过期策略。同时在 PostgreSQL 中增加仅索引 ACTIVE 公告的部分索引，使实际查询目标不受 CLOSED 数据累积影响而更快命中。','최종적으로 10만 건 기준 평균 응답시간은 15,551ms에서 1,118ms로, p95는 28,654ms에서 1,385ms로 줄었고 TPS는 6.3/s에서 85.2/s로 올라갔습니다. 에러율도 1.11%에서 0.00%로 떨어졌고, EXPLAIN ANALYZE 기준 목록 조회 경로는 Sequential Scan 대신 Index Scan 중심으로 전환되었습니다. 즉 캐시로 반복 집계를 줄이고, 부분 인덱스로 ACTIVE 조회 범위를 좁혀 목록 API를 구조적으로 안정화했습니다.':'最终，10 万条数据下平均响应时间从 15,551ms 降至 1,118ms，p95 从 28,654ms 降至 1,385ms，TPS 从 6.3/s 提升至 85.2/s。错误率也从 1.11% 降至 0.00%；根据 EXPLAIN ANALYZE，列表查询路径从 Sequential Scan 转为以 Index Scan 为主。也就是说，通过缓存减少重复聚合，并用部分索引缩小 ACTIVE 查询范围，从结构上稳定了列表 API。',
  '채용 공고 100만 건 목록 조회, 뒤 페이지 응답 시간을 84초에서 0.137초로 단축':'招聘公告 100 万条列表查询：后页响应时间从 84 秒缩短至 0.137 秒','채용 공고 데이터가 100만 건 규모로 커지자, 뒤쪽 페이지로 갈수록 OFFSET 기반 목록 조회가 급격히 느려졌습니다. 특히 deep page에서는 실제 사용자 요청보다 DB가 앞의 수십만 행을 버리는 비용이 더 커져, 몇 초가 아니라 수십 초 단위까지 치솟는 구간이 생겼습니다.':'招聘公告数据增至约 100 万条后，基于 OFFSET 的列表查询越到后页越慢。尤其在 deep page，DB 丢弃前几十万行的成本高于实际用户请求，响应从几秒上升到几十秒。','기존 쿼리는 전체 행을 정렬한 뒤 OFFSET 만큼 건너뛰는 방식이라, 뒤 페이지일수록 버려지는 행의 비용이 그대로 누적됐습니다. 여기에 정렬 동률 시 tie-breaker가 없어 페이지 경계에서 중복·누락 가능성도 있었고, 1단계 조회와 실제 응답 조립 사이에 데이터가 바뀌면 결과 정합성도 흔들릴 수 있었습니다.':'原查询先排序全部行，再跳过 OFFSET 数量的行，因此越到后页，被丢弃行的成本累积越多。此外排序相同值时没有 tie-breaker，页面边界可能重复或遗漏；如果第一阶段查询与实际组装响应之间数据发生变化，结果一致性也会动摇。','API 계약은 유지한 채, 먼저 페이지에 필요한 ID만 커버링 인덱스로 빠르게 뽑고 이후 해당 ID의 실제 행만 읽는 deferred join 2단계 방식으로 바꿨습니다. 또한 모든 정렬에':'在保持 API 契约不变的前提下，先通过覆盖索引快速取出页面所需的 ID，再读取这些 ID 对应的实际行，改为 deferred join 两阶段方案。同时在所有排序中加入','tie-breaker를 추가하고, 2단계 조회에서도 동일 predicate를 다시 적용해 deep pagination에서도 순서와 결과 정합성이 흔들리지 않도록 보완했습니다.':'tie-breaker，并在第二阶段查询中再次应用相同 predicate，确保 deep pagination 下顺序和结果一致性不受影响。','EXPLAIN ANALYZE 기준으로 OFFSET 50만 구간이 약 84초에서 약 137ms 수준으로 줄었고, 80만 구간도 타임아웃 대신 147ms대로 안정화했습니다. 프론트 API를 갈아엎지 않고도 deep page 병목만 정확히 제거해, 성능과 기존 UX를 함께 지킬 수 있었습니다.':'根据 EXPLAIN ANALYZE，OFFSET 50 万的位置从约 84 秒降至约 137ms，80 万的位置也稳定在约 147ms 而不再超时。不改造前端 API，仅精准移除 deep page 瓶颈，同时保住了性能与原有 UX。',
  '구독 자동결제 배치, 1만 건 처리 시간을 64.4초에서 17.5초로 단축':'订阅自动支付批处理：1 万条处理时间从 64.4 秒缩短至 17.5 秒','자동결제 배치는 대상 구독 수가 늘수록 한 건씩 필요한 정보를 다시 조회하고, Toss 결제 호출도 순차로 기다리는 구조였습니다. 배치량이 커지면 처리 완료까지 오래 걸릴 뿐 아니라, 스케줄러가 다음 작업을 미루게 될 위험도 커졌습니다.':'自动支付批处理会随着目标订阅增加而逐条重新查询所需信息，并顺序等待 Toss 支付调用。批量变大不仅会延长完成时间，也会增加调度器推迟下一项任务的风险。','구독, 요금제, 결제 프로필, 회원 정보, 멱등 결제 정보를 루프 안에서 개별 조회해 전형적인 N+1 패턴이 발생하고 있었습니다. 또한 외부 결제 API 호출을 순차 처리하고 있어, DB 병목과 네트워크 대기 시간이 동시에 전체 wall-clock 시간을 늘리고 있었습니다.':'订阅、套餐、支付资料、会员信息和幂等支付信息都在循环中逐条查询，形成典型的 N+1 模式。外部支付 API 也按顺序调用，DB 瓶颈和网络等待同时拉长了 wall-clock 时间。','배치 시작 전에 필요한 연관 데이터를':'在批处理开始前，将所需关联数据','절 배치 조회로 선로딩해':'通过 IN 批量查询预加载到','로 묶고, 결제 생성도 선로딩 결과를 재사용하도록 구조를 바꿨습니다. 그 위에 고정 스레드풀 기반 bounded 병렬화를 적용하되, 멱등키·건당 타임아웃·취소 처리를 넣어 속도를 높이면서도 중복결제와 스케줄러 블로킹을 함께 막았습니다.':'中，并封装为 RenewalContext，支付创建也复用预加载结果。在此基础上使用固定线程池进行 bounded 并行化，同时加入幂等键、单笔超时和取消处理，在提速的同时防止重复支付与调度器阻塞。','건당 SQL 수는 10개에서 6개로 줄었고, 결제 진입 전 반복 조회는 3만 건 수준에서 사실상 한 자릿수 배치 조회로 축소됐습니다. 1만 건 기준 wall-clock은 64.4초에서 17.5초로, Toss 지연이 있는 환경에서도 235.6초에서 13.9초로 크게 단축하면서 결제 정확성은 그대로 유지했습니다.':'每笔 SQL 从 10 个降至 6 个，支付进入前的重复查询从约 3 万次缩减为实际上个位数的批量查询。1 万条的 wall-clock 从 64.4 秒降至 17.5 秒；即使 Toss 存在延迟，也从 235.6 秒降至 13.9 秒，同时保持支付准确性不变。',
  '마은재':'MA RanQian','· 풀스택 신입 개발자 포트폴리오':'· 初级全栈开发者作品集','GitHub 프로필':'GitHub 个人主页','Home':'首页','Table of Contents':'目录','닫기':'关闭','확대 이미지':'放大图片'
};

for (const [from, to] of Object.entries(copy).sort((a, b) => b[0].length - a[0].length)) html = html.split(from).join(to);
html = html.replace(/(<button[^>]*data-filter="auth"[^>]*>)Auth(<\/button>)/g,'$1认证$2')
  .replace(/(<span class="case-label p">)Problem(<\/span>)/g,'$1问题$2')
  .replace(/(<span class="case-label c">)Cause(<\/span>)/g,'$1原因$2')
  .replace(/(<span class="case-label s">)Solution(<\/span>)/g,'$1解决方案$2')
  .replace(/(<span class="case-label r">)Result(<\/span>)/g,'$1结果$2')
  .replace('我将负责 Mapingo AI 会话辅导与 CareerWave 认证、支付领域时遇到的问题','我在负责 Mapingo AI 会话辅导与 CareerWave 认证、支付领域时遇到的问题')
  .replace(/Azure 语音/g,'Azure Speech')
  .replace('<code>/token/refresh</code>调用','<code>/token/refresh</code> 调用')
  .replace('<code>probeAuth()</code>固定顺序','<code>probeAuth()</code> 固定顺序')
  .replace('<code>probeAuth</code>即使','即使调用 <code>probeAuth</code>')
  .replace('<code>countActiveJobNotices()</code>, <code>countTodayNewActiveJobNotices()</code>','<code>countActiveJobNotices()</code>、<code>countTodayNewActiveJobNotices()</code>')
  .replace('<code>JobNoticeCacheService</code> ，','<code>JobNoticeCacheService</code>，')
  .replace('， <code>@CacheEvict</code> 我没有使用 ，改为 TTL 过期策略','，我没有使用 <code>@CacheEvict</code>，改为 TTL 过期策略')
  .replace('所需关联数据 <code>IN</code> 通过 IN 批量查询预加载到 <code>RenewalContext</code>中，并封装为 RenewalContext','所需关联数据通过 <code>IN</code> 批量查询预加载到 <code>RenewalContext</code> 中');
html = html.replace('<html lang="ko">','<html lang="zh-CN">')
  .replace(/<title>.*?<\/title>/,'<title>问题排查 | MA RanQian 作品集</title>')
  .replace(/\.\.\/ko\//g,'../zh/')
  .replace(/<div class="i18n-switcher"[\s\S]*?<\/div>/,'<div class="i18n-switcher" aria-label="Language"><a href="../ko/troubleshooting.html">한국어</a><a href="../zh/troubleshooting.html" aria-current="page">中文</a><a href="../en/troubleshooting.html">English</a></div>')
  .replace(/<nav class="toc"[\s\S]*?<\/nav>/, `<nav class="toc" aria-label="目录">
    <a href="#cases">问题案例</a>
    <a href="#auth">认证</a>
    <a href="#billing">支付</a>
    <a href="#ai">AI · LLM</a>
    <a href="#speech">语音</a>
    <a href="#performance">性能</a>
</nav>`)
  .replace('</head>','<meta http-equiv="Cache-Control" content="no-store">\n</head>');
await writeFile(join(root, 'zh/troubleshooting.html'), html);
console.log('Generated Chinese troubleshooting page.');
